using UnityEngine;
using System.Collections;

public class Federationfigher : MonoBehaviour {
	public GameObject figter;
	// Use this for initialization
	void Start () {
		figter.transform.Rotate (0,0,0);
	}
	
	// Update is called once per frame
	void Update () {
		figter.transform.Rotate (0,0,0);
	}
}
